version: '3.4'

services:
  wallet-server:
    build: ./api
    ports:
      - "5555:5555"
    restart: always
  auction-client:
    build: ./auction-client
    ports:
        - "3001:80"
    restart: always
  wallet-client:
    build: ./wallet-management
    ports:
        - "3000:80"
    restart: always
  dms:
    build: ./dms
    ports:
        - "3002:80"
    restart: always
